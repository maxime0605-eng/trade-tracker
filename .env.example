# Copiez ce fichier en .env (jamais commité)
# DATABASE_URL : récupéré dans Railway > PostgreSQL plugin > Connect > Connection String
DATABASE_URL=postgresql://user:password@host:port/dbname

JWT_SECRET=changez-cette-valeur
NODE_ENV=development
PORT=3001
